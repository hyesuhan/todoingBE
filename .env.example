# DB (docker-compose.yml의 postgres 서비스 기준)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/todoing
DB_USERNAME=todoing
DB_PASSWORD=todoing

# Redis (docker-compose.yml 기준 — IDE/터미널에서 직접 실행할 땐 localhost, 컨테이너 안에서 실행할 땐 redis)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=
JWT_ACCESS_EXPIRE=3600000
JWT_REFRESH_EXPIRE=1209600000

# OpenAI (Spring AI) - 현재 OpenAiService는 아래 NVIDIA 쪽을 실제로 호출함, 이 값은 spring.ai.openai 자동설정용(미사용)
OPENAI_API_KEY=

# NVIDIA NIM (build.nvidia.com) - OpenAI 호환 API, 챗봇이 실제로 호출하는 곳
LLAMA_API_KEY=

# Kakao Login
KAKAO_CLIENT_ID=
KAKAO_REDIRECT_URI=http://localhost:8080/api/users/login/kakao

# Kakao Pay
KAKAOPAY_CID=
KAKAOPAY_SECRET_KEY=

# Google Cloud Vision / Speech (google-cloud-vision, google-cloud-speech)
# 서비스 계정 JSON 키 파일 경로 (Application Default Credentials)
GOOGLE_APPLICATION_CREDENTIALS=
